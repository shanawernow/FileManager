<%= form_with(model: datum, url: datum.new_record? ? user_data_path(datum.user) : datum_path(datum)) do |form| %>
  <% if datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(datum.errors.count, "error") %> prohibited this datum from being saved:</h2>

      <ul>
      <% datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filename %>
    <%= form.text_field :filename, id: :datum_filename %>
  </div>

  <div class="field">
    <%= form.label :filesize %>
    <%= form.text_field :filesize, id: :datum_filesize %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
